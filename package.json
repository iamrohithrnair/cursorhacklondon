{
  "name": "t-1000",
  "version": "1.0.0",
  "description": "Autonomous Software Factory for Cursor IDE",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npx tsx src/server.ts",
    "heal": "npx tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "setup": "npm install && npx playwright install chromium",
    "reset": "git checkout main && git checkout -- src/components/"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@sentry/node": "^10.47.0"
  }
}
